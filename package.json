{
  "name": "elementary",
  "version": "1.0.0",
  "description": "An aggressive reset style sheet for modern web browsers",
  "keywords": [
    "css",
    "css3",
    "reset",
    "stylesheet"
  ],
  "author": "Kieran Potts <hello@kieranpotts.com> (https://www.kieranpotts.com/)",
  "license": "MIT",
  "homepage": "https://www.elementarycss.com/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kieranpotts/elementary.git"
  },
  "bugs": {
    "url": "https://github.com/kieranpotts/elementary/issues"
  },
  "engines": {
    "node": ">=4.5.0"
  },
  "dependencies": {
    "autoprefixer": "^7.1.1",
    "chokidar-cli": "^1.2.0",
    "cssnano": "^3.9.1",
    "node-sass-chokidar": "^0.0.1",
    "npm-run-all": "^4.0.2",
    "postcss-cli": "^4.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "build:compile": "node-sass-chokidar --source-map true src/ --output build/",
    "build:autoprefix": "postcss build/elementary.css --use autoprefixer --output build/elementary.css",
    "build:minify": "postcss build/elementary.css --no-map --use cssnano --output build/elementary.min.css",
    "sass:build": "run-s build:*",
    "sass:watch": "chokidar src/elementary.scss --command \"npm run sass:build\" --throttle 10000",
    "dev": "run-s sass:*"
  }
}
